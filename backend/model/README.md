Model artifacts are generated by the training pipeline in `../ml/`.

Expected files:

- `fake_news_model.pkl`
- `vectorizer.pkl`

The backend falls back to a demo heuristic if these files are missing.

