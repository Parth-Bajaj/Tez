# Dataset Notes

The `dataset/` folder stores a lightweight sample dataset for development.

- `raw/` contains the original fake and true news CSV files
- `cleaned/` stores cleaned output
- `train/` and `test/` hold train/test splits

Replace the sample rows with the full dataset before serious model training.

